import React from 'react'
import ButtonMU from '@material-ui/core/Button'

export const Button = (props) => {
    const {type, variant, color, onClick} = props;
    return(
        <ButtonMU>
        </ButtonMU>

    )
}

