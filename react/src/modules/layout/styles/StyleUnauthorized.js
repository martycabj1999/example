import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    typography:{
        textAlign: 'center',
        margin:'auto',

    },
    box:{
        padding: 0,
        height:"71vh",
        margin:'auto',
        marginTop:50
    },

}));